json.extract! enrollment, :id, :date_of_enrollment, :method_of_contact, :remarks, :coordinator_id, :registry_id, :participant_id, :created_at, :updated_at
json.url enrollment_url(enrollment, format: :json)
